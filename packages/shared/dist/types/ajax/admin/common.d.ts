import { httpRequest } from '../../types';
import VoHttp from '../ajax';
export default class Common extends VoHttp {
    constructor(props: any);
    sixHotAreaSub(data?: {}, option?: httpRequest): any;
}
